version: '3.8'

services:
   redis:
      image: redis:alpine
      container_name: nodets-redis
      restart: always
      ports:
         - '${REDIS_PORT:-6379}:6379'
      volumes:
         - redis_data:/data

volumes:
   redis_data:
